# Mantle Network RPC
VITE_RPC_URL=https://rpc.mantle.xyz

# Deployed contract address (fill after: make deploy-testnet)
VITE_CONTRACT_ADDRESS=

# Optional: FastAPI URL for fallback data (when running the agent locally)
VITE_API_URL=http://localhost:8000
